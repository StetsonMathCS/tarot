package com.stetson.controller.interfaces;

/** All constants in this interface should be replaced by a function (generating that value),
 * or a database-queried value (e.g. hashedPassword, etc.).
 */
public interface ITemporaryConstants {
    //TODO: migrate to db as hashed value!! (but not our assignment?)
    String TEST_PWD = "12345";
    String SESS_TOKEN = "1s456ds465ds45ds45d6f";
}
